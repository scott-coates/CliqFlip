@model CliqFlip.Infrastructure.Web.LinkDetails

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <title>Index</title>
</head>
<body>
    <div>
        @if(Model != null)
        {
            <div>
                Title: <strong>@Model.Title</strong>
            </div>
            <div>
                Description: <strong>@Model.Description</strong>
            </div>
            <div>
                Image: <br />
                <img src="@Model.ImageUrl" />
            </div>
            <div>
                <iframe src="@Model.VideoUrl" width="300px"></iframe>
            </div>
        }
    </div>
</body>
</html>
