var CliqFlip = (function(cliqFlip) {

    cliqFlip.App.Mvc.Views.PostOverviewUserAcitivtyView = Backbone.Marionette.ItemView.extend({
        template: "feed-postOverviewUserAcitivty"
    });

    return cliqFlip;
} (CliqFlip));